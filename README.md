# Advent of Code 2025

This year with zig.

To run one day, call

`zig build run -Day=1`

To run with optimizations:

`zig build run -Doptimize=ReleaseFast -Day=2`

To run the tests for a day:

`zig test src/day01.zig`
